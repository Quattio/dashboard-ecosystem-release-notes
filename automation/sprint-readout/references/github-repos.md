# Product repos (reference only — no GitHub connector in the cloud routine)

The cloud routine has no GitHub access; derive releases from Jira `fixVersions`.
Kept for context / team attribution.

- **Quattio/cic-yocto-builder** — CIC firmware (Embedded Systems). Releases `CiC - V<x.y.z>`, also Wireless Platform (dongle) + Controller bumps.
- **Quattio/Quatt-cloud** — Backend/Cloud (Cloud / Backend). `fixVersion` like `Cloud v2.x.0`.
- **Quattio/quatt-mobile-app** — Mobile app (APP). `fixVersion` like `App v1.x.0`.

Cross-cutting fixVersions seen in QPD: `Controller <x.y.z>` (Systems Control), `Wireless Platform - V<x.y.z>` (dongle, Embedded).
